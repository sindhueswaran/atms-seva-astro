---
import type { HTMLAttributes } from "astro/types";
type Props = HTMLAttributes<"h2">;
const { class: className, ...rest } = Astro.props;
---

<h2
  class={`text-title font-display font-bold text-amber-500 text-4xl ${className}`}
  {...rest}
>
  <slot />
</h2>
